## Dev Container Usage Example

Open this folder in VS Code and then just click on "Reopen in Container". Then open a new terminal window (in VS Code) and type:
```
go version
```

Now you have a Go language development container running.
